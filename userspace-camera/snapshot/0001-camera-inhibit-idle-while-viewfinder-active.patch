From 375d12716f18943e9a11aad3b5c082bd9fd1fb26 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Lajosh=C3=A1zi=2C=20L=C3=A1szl=C3=B3=20Gergely?=
 <lajoshazilg@gmail.com>
Date: Tue, 21 Jul 2026 06:10:00 +0200
Subject: [PATCH] camera: inhibit idle while the viewfinder is active

The idle inhibitor was only held while recording a video, so leaving the
viewfinder open let the display blank and lock mid-capture. Tie the inhibitor
to the viewfinder state instead, so the screen stays on the whole time the
camera is live (ViewfinderState::Ready); recording is a subset of that, so
the previous recording-only inhibit is removed.

Only ApplicationInhibitFlags::IDLE is used (unchanged), so this prevents the
screen blanking/locking, not system suspend.

On mobile (e.g. postmarketOS/phosh on a Fairphone 3) the idle timeout is
short (~90 s), so this bites during normal use: the screen locks while
framing a shot in the viewfinder.

Closes #366
---
 src/widgets/camera.rs | 19 +++++++++++++++----
 1 file changed, 15 insertions(+), 4 deletions(-)

diff --git a/src/widgets/camera.rs b/src/widgets/camera.rs
index baebfb2..8526d5e 100644
--- a/src/widgets/camera.rs
+++ b/src/widgets/camera.rs
@@ -210,15 +210,11 @@ mod imp {
                 #[weak]
                 obj,
                 move |viewfinder| {
-                    let window = viewfinder.root().and_downcast::<crate::Window>().unwrap();
-
                     if viewfinder.is_recording() {
                         obj.set_shutter_mode(crate::ShutterMode::Recording);
-                        window.inhibit("Recording Video");
                         obj.show_recording_label();
                     } else {
                         obj.hide_recording_label();
-                        window.uninhibit();
                         if matches!(obj.shutter_mode(), crate::ShutterMode::Recording) {
                             obj.set_shutter_mode(crate::ShutterMode::Video);
                         }
@@ -226,6 +222,21 @@ mod imp {
                 }
             ));
 
+            // Inhibit idle (screen blanking and locking) the whole time the
+            // viewfinder is active, not only while recording, so the display
+            // does not turn off mid-capture. See GNOME/snapshot#366.
+            self.viewfinder.connect_state_notify(|viewfinder| {
+                let Some(window) = viewfinder.root().and_downcast::<crate::Window>() else {
+                    return;
+                };
+
+                if matches!(viewfinder.state(), aperture::ViewfinderState::Ready) {
+                    window.inhibit("Camera viewfinder is active");
+                } else {
+                    window.uninhibit();
+                }
+            });
+
             self.selection.set_model(Some(provider));
             self.selection.connect_selected_item_notify(glib::clone!(
                 #[weak]
-- 
2.53.0

